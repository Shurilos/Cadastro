@model IEnumerable<WebApplication1.Models.Usuario>

@{
    ViewData["Title"] = "Usuários";
}

<div class="d-flex align-items-center mb-4 gap-2">
    <a asp-controller="Home" asp-action="Index" class="btn btn-sm btn-outline-secondary">← Início</a>
    <h4 class="mb-0 ms-2 fw-semibold">👤 Usuários</h4>
    <a asp-action="Create" class="btn btn-primary btn-sm ms-auto">+ Novo Usuário</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">Nenhum usuário cadastrado ainda.</div>
}
else
{
    <div class="card shadow-sm border-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>Nome</th>
                        <th>E-mail</th>
                        <th>CPF</th>
                        <th>Perfil</th>
                        <th>Cadastro</th>
                        <th class="text-center">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Nome @item.Sobrenome</td>
                            <td>@item.Email</td>
                            <td>@item.Cpf</td>
                            <td><span class="badge bg-secondary">@item.Perfil</span></td>
                            <td>@item.DataCadastro.ToString("dd/MM/yyyy")</td>
                            <td class="text-center">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Ver</a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning">Editar</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Excluir</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}
